# ketin

Relational compile time node/value/type collections. Specifically abstractions over `macrocache` (which works with IC) so that users do not have to write any macros themselves.

This is maybe the coolest thing I have done with this language. However there is still some missing functionality and the API is not final. See tests for what currently works.
